

    (************************************)
    (*   Never edit options files when  *)
    (*       the daemon is running      *)
    (************************************)
    (* SECTION : Fasttrack *)
    (* Fasttrack options *)
    (************************************)



	(* Maximal number of ultrapeers connected on Fasttrack *)
 max_ultrapeers = 5

	(* The port to bind the client to *)
	(* changing this option requires restart of MLDonkey core *)
 client_port = 1214

	(* Activate this option if you want to test experimental features. *)
 experimental = false

	(* A list of GWCache urls *)
 hostfiles = [
  "http://cache.kicks-ass.net:8000/";
  "http://g2cache.theg2.net/gwcache/lynnx.asp";
  "http://gwcrab.sarcastro.com:8001";
  "http://gwebcache.bearshare.net/";
  "http://gwebcache.bearshare.net/gcache.php";
  "http://pokerface.ibiza.bishopston.net:3558/";
  "http://t.az.is.teh.r0x0r.gtkg.de/";
  "http://tribaldance.ibiza.bishopston.net:3558/";]

	(* A list of urlfile urls *)
 urlfiles = []

	(* The hosts to connect to to get a list of peers *)
 redirectors = []

	(* Maximal number of ultrapeers remembered *)
 max_known_ultrapeers = 500

	(* Maximal number of peers remembered *)
 max_known_peers = 20

	(* timeout when connecting to a server *)
 server_connection_timeout = 30.

	(* The UID of this client *)
 client_uid = AC87F0FBB40E296FFF341E5866717000

	(* The maximal number of slots for upload by Fasttrack clients *)
 max_available_slots = 5

	(* Under this threshold, files are verified using the SHA1 digest
	(always available), without using the tiger-tree (experimental). *)
 sha1_verification_threshold = 0

	(* (only for development tests) *)
 deflate_connections = false

	(* (only for development tests) *)
 keep_alive = true

	(* (only for development tests) *)
 dont_connect = false

	(* (only for development tests) *)
 supernode_enabled = false

	(* (only for development tests) *)
 incoming_data_log = ""

	(* (internal option) *)
	(* Do not change this option, internal use only! *)
 options_version = 0
 old_files = []
 cache = {

	(* Known Peers *)
   peers = []

	(* Known ultrapeers *)
   ultrapeers = []
}

(*
 The following options are not used (errors, obsolete, ...) 
*)
